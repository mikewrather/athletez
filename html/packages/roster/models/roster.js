//City Model

define([ 'models', 'facade' ], function(models, facade) {

	var RostereModel, BaseModel = models.BaseModel;
	var $ = facade.$;
	var _ = facade._;
	RostereModel = BaseModel.extend({
	});

	return RostereModel;
});