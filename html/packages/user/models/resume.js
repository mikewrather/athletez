// The User Resume Model
// -----------
// Requires define
// Return {Resume Model} object as constructor  

define(["facade", "models/base"], function (facade, BaseModel) {

    var Model,
        _ = facade._;

    Model = BaseModel.extend({  
    });
    return Model;
});
