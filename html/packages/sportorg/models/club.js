// Sportorg Club Model
// -----------
// Requires define
// Return {ClubModel} object as constructor  

define(["facade", "models/base"], function (facade, BaseModel) {

    var ClubModel,
        _ = facade._;

    ClubModel = BaseModel.extend({  

              
        
    });

    return ClubModel;
});
