// Home Image Model

define([ "facade", "media/models/image" ], function(facade, BaseModel) {

	var HomeImageModel, _ = facade._;

	HomeImageModel = BaseModel.extend({

	});

	return HomeImageModel;
});
