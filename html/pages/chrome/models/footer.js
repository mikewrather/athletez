// footer.js Model
// ------------
// Requires define
// Return {BaseModel} model constructor object

define( ["models/base"], function (BaseModel) {

    var FooterModel;

    FooterModel = BaseModel.extend({

        defaults: {
                        
        }
        
    });

    return FooterModel;
});

