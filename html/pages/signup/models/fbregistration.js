define( ["models/base"], function (BaseModel) {


	var MainModel;

    MainModel = BaseModel.extend({

        defaults: {
                        
        }//,
       //url:'api/user/register' 
        
    });

    return MainModel;

});