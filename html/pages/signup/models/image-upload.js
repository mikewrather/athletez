define( ["models/base"], function (BaseModel) {


	var MainModel;

    MainModel = BaseModel.extend({

        defaults: {
                        
        },
       url:'/api/user/savecrop/425983',
       payload:{} 
        
    });

    return MainModel;

});