// Site Comment Model
// -----------
// Requires define
// Return {SiteCommentModel} object as constructor  

define(["facade", "models/base"], function (facade, BaseModel) {

    var SitePhraseModel,
        _ = facade._;

    SitePhraseModel = BaseModel.extend({

        
    });

    return SitePhraseModel;
});
